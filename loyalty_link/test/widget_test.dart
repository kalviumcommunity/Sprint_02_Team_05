// Widget tests are skipped for Firebase-dependent screens
// because Firebase requires native initialization.
// Use integration tests or manual testing for auth + Firestore flows.

void main() {
  // Placeholder – Firebase widget tests require mockFirebaseApp setup.
}
